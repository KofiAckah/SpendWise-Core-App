################################################################################
# SpendWise Application Environment Configuration
################################################################################
# 
# IMPORTANT: Do NOT commit .env to version control!
# This is a template file. Follow these steps:
#   1. Copy this file: cp .env.example .env
#   2. Update the values in .env with your actual configuration
#   3. Never commit .env to Git (add .env to .gitignore)
#
################################################################################

#------------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
#------------------------------------------------------------------------------

# POSTGRES_DB - Database name
POSTGRES_DB=spendwise

# POSTGRES_USER - Database superuser username
POSTGRES_USER=postgres

# POSTGRES_PASSWORD - Database superuser password
# SECURITY: Use a strong password in production!
POSTGRES_PASSWORD=postgres123

#------------------------------------------------------------------------------
# Backend API Configuration
#------------------------------------------------------------------------------

# PORT - Backend API server port
PORT=5000

# DB_HOST - Database host (use 'postgres' for Docker, 'localhost' for local dev)
DB_HOST=postgres

# DB_PORT - Database port
DB_PORT=5432

# DB_NAME - Database name (should match POSTGRES_DB)
DB_NAME=spendwise

# DB_USER - Database user for backend (should match POSTGRES_USER)
DB_USER=postgres

# DB_PASSWORD - Database password for backend (should match POSTGRES_PASSWORD)
DB_PASSWORD=postgres123

#------------------------------------------------------------------------------
# Frontend Configuration
#------------------------------------------------------------------------------

# VITE_API_URL - Backend API base URL
# Development: http://localhost:5000
# Production: https://api.spendwise.example.com
# Docker: http://backend:5000 (if using container networking)
VITE_API_URL=http://localhost:5000

#------------------------------------------------------------------------------
# Docker Configuration
#------------------------------------------------------------------------------

# COMPOSE_PROJECT_NAME - Docker Compose project name
# This helps isolate containers when running multiple projects
COMPOSE_PROJECT_NAME=spendwise

################################################################################
# End of Configuration
################################################################################
