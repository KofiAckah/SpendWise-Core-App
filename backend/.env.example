################################################################################
# SpendWise Backend Environment Configuration
################################################################################
# 
# IMPORTANT: Do NOT commit this file to version control!
# This is a template file. Follow these steps:
#   1. Copy this file: cp .env.example .env
#   2. Update the values in .env with your actual credentials
#   3. Never commit .env to Git (it's in .gitignore)
#
################################################################################

#------------------------------------------------------------------------------
# Server Configuration
#------------------------------------------------------------------------------

# PORT - The port number on which the Express server will listen
# Default: 5000
# Used by: Express server (index.js)
# Example: PORT=3000
PORT=5000

#------------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
#------------------------------------------------------------------------------

# DB_HOST - PostgreSQL server hostname or IP address
# Default: postgres (Docker container name)
# Production: Use actual database server hostname/IP
# Development with Docker: postgres
# Development local: localhost
DB_HOST=postgres

# DB_PORT - PostgreSQL server port
# Default: 5432 (PostgreSQL default port)
# Note: Usually no need to change unless using non-standard port
DB_PORT=5432

# DB_NAME - Name of the PostgreSQL database
# Default: spendwise
# This database will be created by init.sql if it doesn't exist
DB_NAME=spendwise

# DB_USER - PostgreSQL username for authentication
# Default: postgres
# Security: Use a dedicated user with limited privileges in production
# Example: DB_USER=spendwise_app
DB_USER=postgres

# DB_PASSWORD - PostgreSQL user password
# Default: postgres123
# SECURITY WARNING: Change this to a strong password!
# Requirements for production:
#   - Minimum 16 characters
#   - Mix of uppercase, lowercase, numbers, special characters
#   - Never use default passwords in production
# Example: DB_PASSWORD=Xy9$mK2#pL5@qR8&nW3!
DB_PASSWORD=postgres123

#------------------------------------------------------------------------------
# Optional: Logging Configuration
#------------------------------------------------------------------------------

# LOG_LEVEL - Application log verbosity (if implementing custom logging)
# Options: debug, info, warn, error
# Default: info
# Uncomment to use:
# LOG_LEVEL=info

#------------------------------------------------------------------------------
# Optional: CORS Configuration
#------------------------------------------------------------------------------

# CORS_ORIGIN - Allowed origins for Cross-Origin Resource Sharing
# Default: * (all origins - not recommended for production)
# Production: Specify exact frontend URL
# Example: CORS_ORIGIN=https://spendwise.example.com
# Uncomment to use:
# CORS_ORIGIN=http://localhost:5173

#------------------------------------------------------------------------------
# Optional: Security Configuration
#------------------------------------------------------------------------------

# NODE_ENV - Application environment
# Options: development, production, test
# Default: development
# Used for: Conditional behavior (error details, logging, etc.)
# Uncomment to use:
# NODE_ENV=development

################################################################################
# End of Configuration
################################################################################
